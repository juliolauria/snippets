### conda enviroment ###

# Create a new conda environment with specific python version
conda create --name 'env_name' python='python_version'

# Activate the environment
conda activate 'env_name'

# Remove a conda environment 
conda env remove --name bio-env

# List all conda environments 
conda env list

# List all packages from a environment 
conda list

# Install pip
conda install pip

# Install libraries using the requirements.txt file
pip install -r requirements.txt

### ipykernel ###

# Installing a kernel
ipykernel install --user --name 'env_name' --display-name 'kernel_name' 

# List all installed kernels
jupyter kernelspec list

# Remove a kernel
jupyter kernelspec uninstall unwanted-kernel
